[project]
name = "gruenbeck-softliq-mc-tools"
version = "0.1.3"
description = "Development tools for the Gruenbeck softliQ MC Home Assistant integration"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.9.0",
    "xmltodict>=0.13.0",
]

[tool.uv]
package = false
